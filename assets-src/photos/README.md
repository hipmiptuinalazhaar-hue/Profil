# Official photo source bundle

Binary photo assets for HIPMI PT are committed through Git data and materialized into `public/photos` for production. Canonical filenames are defined in `src/data/photo-manifest.ts`.
